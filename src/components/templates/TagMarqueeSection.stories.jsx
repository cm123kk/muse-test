import TagMarqueeSection from './TagMarqueeSection';

export default {
  title: 'Section/TagMarqueeSection',
  component: TagMarqueeSection,
  tags: ['autodocs'],
  parameters: { layout: 'fullscreen' },
};

/* T1 tag 3-row marquee: direction alternates per row (left/right/left), oversized tags */
export const Default = {};
